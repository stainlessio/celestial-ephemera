language: objective-c
before_script:
  - brew update && brew install tbb
  - mkdir build
  - cd build
  - cmake -G Xcode ..
  - cp -R ../support/xcode/xcshareddata celestial.xcodeproj/
xcode_project: celestial.xcodeproj
xcode_scheme: ALL_BUILD
